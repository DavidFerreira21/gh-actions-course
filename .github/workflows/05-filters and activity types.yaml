name: 05 - Events filters and activity types
on:
    pull_request:
      types:
        - closed
      branches:
        - main
jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: |
           echo "Event name: ${{ github.event_name }}"