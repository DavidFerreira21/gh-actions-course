name: 02 - Workflow Events
on:
    workflow_dispatch
    # push:
    #   branches:
    #     - main
    # pull_request:
    #   branches:
    #     - main
jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: |
           echo "Event name: ${{ github.event_name }}"